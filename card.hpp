#include <iostream>


using namespace std;

class Card 
{
    private:
        string suit;
        string value;
        bool flipped;

    public:
        Card(string _suit, string _value){ suit = _suit; value = _value; }
        bool flip() { flipped != flipped; }

};  